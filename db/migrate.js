// Миграция из Google Sheets — устарело
// Зарезервировано для будущего импорта данных

export async function migrateFromSheets(_onProgress) {
  return { success: false, message: 'Функция недоступна в текущей версии' };
}
