// Синхронизация с внешними сервисами — отключена
// Зарезервировано для будущей интеграции (облачный бэкап, API)

export function startAutoSync(_intervalMs) {
  // no-op
  console.log('[SYNC] Автосинхронизация запущена (каждые 30с)');
}

export function stopAutoSync() {
  // no-op
}
